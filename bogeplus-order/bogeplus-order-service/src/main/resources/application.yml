server:
  port: 18701
spring:
  application:
    name: bogeplus-order
  cloud:
    nacos:
      discovery:
        server-addr: localhost
        cluster-name: BJ

  # mysql
  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://119.3.230.36:3306/massage_order?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    #    username: rw_user
    #    password: Qq123123
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/massage_order?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root

    # redis
  redis:
      host: 119.3.230.36
      port: 6379
      password: Aa123123123 # 如果 Redis 没有设置密码，可以省略这一行
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms
      timeout: 3000ms


mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: is_deleted # 全局逻辑删除字段名
      logic-delete-value: 1 # 逻辑已删除值
      logic-not-delete-value: 0 # 逻辑未删除值
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
logging:
  level:
    com.bogeplus.user.mapper: debug


seata:
  enabled: true
  enable-auto-data-source-proxy: true
  registry: # TC服务注册中心的配置，微服务根据这些信息去注册中心获取tc服务地址
    type: nacos # 注册中心类型 nacos
    nacos:
      server-addr: localhost:8848 # nacos地址
      namespace: "" # namespace，默认为空
      group: DEFAULT_GROUP # 分组，默认是DEFAULT_GROUP
      application: seata-tc-server # seata服务名称
      username: nacos
      password: nacos
      cluster: BJ
  tx-service-group: bogeplus # 事务组名称
  service:
    vgroup-mapping: # 事务组与tc集群的映射关系
      bogeplus: BJ






<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bogeplus.order.mapper.OrderEvaluationMapper">

    <select id="querySumRatingById" resultType="java.math.BigDecimal">
        SELECT SUM(moe.rating) AS total_rating
        FROM massage_order_info moi
                 LEFT JOIN massage_order_evaluation moe ON moi.id = moe.order_id
        WHERE moi.massagist_id = ${id}
          and moi.evaluated = 1
    </select>
</mapper>
